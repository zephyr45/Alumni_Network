<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:openDrawer="start"
    tools:context=".Home">

    <include
        android:id="@+id/appBarMain"
        layout="@layout/appbarmain"
        />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:itemBackground="@drawable/navbottomcornerreadius"
        android:background="@color/white"
        app:headerLayout="@layout/navheader"
        app:itemIconTint="@color/icontextcolorstateforsidenav"
        app:itemRippleColor="@color/lightgrey"
        app:itemTextColor="@color/icontextcolorstateforsidenav"
        app:menu="@menu/mav_menu"
        app:itemShapeFillColor="@color/lightgrey"
        />



</androidx.drawerlayout.widget.DrawerLayout>
